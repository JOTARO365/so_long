# ─── Build artifacts ──────────────────────────────────────────
*.o
*.a
*.d

# Compiled binaries
/so_long
/so_long_bonus

# ─── Third-party / heavy files ────────────────────────────────
# MiniLibX is git-cloned at build time by the Makefile (from 42Paris),
# so neither the extracted source tree nor the tarballs are tracked.
minilibx-linux/
minilibx-linux.tgz
minilibx_macos_metal.tgz
minilibx_macos_opengl.tgz

# ─── Reference material (kept on disk, not committed) ──────────
en.subject.pdf
CLAUDE.md
docs/*.jpg
docs/*.gif

# ─── Dev-only generation assets (kept on disk, not committed) ──
assets/
fix_headers.py

# ─── Tooling / editor / OS junk ───────────────────────────────
.claude/
.DS_Store
*.swp
*~
*.bak
